# CI config for shell runner

stages:
  - build
  - test
  - release

build:
  stage: build
  script:
    - make

vet:
  stage: test
  needs: []
  script:
    - go fmt ./...
    - go vet ./...

test:
  stage: test
  needs: []
  script:
    - go test -race ./...

podman:
  stage: release
  script:
    - podman build -t pproj/tutter .
    - podman push pproj/tutter
  only:
    - master
